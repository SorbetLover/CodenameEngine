name: Linux Builds

on:
  push:
  workflow_dispatch:

jobs:
  build:
    if: contains(github.event.head_commit.message, '&build')

    name: Linux Build
    permissions: write-all
    runs-on: ubuntu-24.04
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v4
        
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.7
          
      - name: Configurar Haxelib
        run: |
          # Garantir que o diretório haxelib existe
          mkdir -p ~/haxelib
          haxelib setup ~/haxelib
          
      - name: Restore existing build cache for faster compilation
        uses: actions/cache@v4.2.3
        with:
          key: cache-build-linux
          path: |
            ~/.haxelib/
            ~/.hxcpp/
            export/release/linux/haxe/
            export/release/linux/obj/
            
      - name: Configurar variáveis de ambiente
        run: |
          echo "HXCPP_COMPILE_THREADS=4" >> $GITHUB_ENV
          echo "HXCPP_COMPILE_CACHE=$HOME/.hxcpp" >> $GITHUB_ENV
          
      - name: Instalar e configurar HXCPP
        run: |
          # Instalar hxcpp se não existir
          haxelib install hxcpp
          
          # Reconstruir hxcpp para garantir que as bibliotecas estão disponíveis
          haxelib run hxcpp rebuild
          
          # Verificar se as bibliotecas foram criadas
          ls -la ~/.hxcpp/ || true
          ls -la ~/.hxcpp/hxcpp_zlib_impl/lib/ || true
          ls -la ~/.hxcpp/hxcpp_zip_api/lib/ || true
          
      - name: Installing LibVLC
        run: |
          sudo apt-get update
          sudo apt-get install -y libvlc-dev libvlccore-dev
          
      - name: Installing/Updating libraries
        run: |
          haxe -cp commandline -D analyzer-optimize --run Main setup -s
          
      - name: Building the game
        run: |
          # Limpar builds anteriores
          haxelib run lime clean linux
          
          # Build release
          haxelib run lime build linux -release
          
      - name: Uploading artifact (executable)
        uses: actions/upload-artifact@v4
        with:
          name: Codename Engine (Executable Only)
          path: export/release/linux/bin/CodenameEngine
          
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v4
        with:
          name: Codename Engine
          path: export/release/linux/bin/
          
      - name: Clearing already existing cache
        uses: actions/github-script@v6
        with:
          script: |
            const caches = await github.rest.actions.getActionsCacheList({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            for (const cache of caches.data.actions_caches) {
              if (cache.key == "cache-build-linux") {
                console.log('Clearing ' + cache.key + '...')
                await github.rest.actions.deleteActionsCacheById({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  cache_id: cache.id,
                })
                console.log("Cache cleared.")
              }
            }
            
      - name: Uploading new cache
        uses: actions/cache@v4.2.3
        with:
          key: cache-build-linux
          path: |
            ~/.haxelib/
            ~/.hxcpp/
            export/release/linux/haxe/
            export/release/linux/obj/

  debug_build:
    if: contains(github.event.head_commit.message, '&builddebug')
  
    name: Linux Debug Build
    permissions: write-all
    runs-on: ubuntu-24.04
    needs: build
    steps:
      - name: Pulling the new commit
        uses: actions/checkout@v4
        
      - name: Setting up Haxe
        uses: krdlab/setup-haxe@v2
        with:
          haxe-version: 4.3.7
          
      - name: Configurar Haxelib
        run: |
          mkdir -p ~/haxelib
          haxelib setup ~/haxelib
          
      - name: Restore existing build cache for faster compilation
        uses: actions/cache@v4.2.3
        with:
          key: cache-build-linux-debug
          path: |
            ~/.haxelib/
            ~/.hxcpp/
            export/debug/linux/haxe/
            export/debug/linux/obj/
            
      - name: Configurar variáveis de ambiente
        run: |
          echo "HXCPP_COMPILE_THREADS=4" >> $GITHUB_ENV
          echo "HXCPP_COMPILE_CACHE=$HOME/.hxcpp" >> $GITHUB_ENV
          
      - name: Instalar e configurar HXCPP
        run: |
          haxelib install hxcpp
          haxelib run hxcpp rebuild
          
      - name: Installing LibVLC
        run: |
          sudo apt-get update
          sudo apt-get install -y libvlc-dev libvlccore-dev
          
      - name: Installing/Updating libraries
        run: |
          haxe -cp commandline -D analyzer-optimize --run Main setup -s
          
      - name: Building the game
        run: |
          haxelib run lime clean linux
          haxelib run lime build linux -debug
          
      - name: Uploading artifact (entire build)
        uses: actions/upload-artifact@v4
        with:
          name: Codename Engine Debug
          path: export/debug/linux/bin/
          
      - name: Clearing already existing cache
        uses: actions/github-script@v6
        with:
          script: |
            const caches = await github.rest.actions.getActionsCacheList({
              owner: context.repo.owner,
              repo: context.repo.repo,
            })
            for (const cache of caches.data.actions_caches) {
              if (cache.key == "cache-build-linux-debug") {
                console.log('Clearing ' + cache.key + '...')
                await github.rest.actions.deleteActionsCacheById({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  cache_id: cache.id,
                })
                console.log("Cache cleared.")
              }
            }
            
      - name: Uploading new cache
        uses: actions/cache@v4.2.3
        with:
          key: cache-build-linux-debug
          path: |
            ~/.haxelib/
            ~/.hxcpp/
            export/debug/linux/haxe/
            export/debug/linux/obj/
